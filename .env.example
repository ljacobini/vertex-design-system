# Vertex Design System — env template
# NEVER commit real secrets. Copy to .env.local and fill.
# .env.local is in .gitignore (R-VPCL-LL-S17-01).

# Optional: Storybook deploy keys
CHROMATIC_PROJECT_TOKEN=CHANGE_ME

# Optional: NPM publish (if private registry)
NPM_TOKEN=CHANGE_ME

{
  "name": "vertex-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Vertex shared design system: tokens, ui, api-client, auth-pattern. Consumed cross-progetto via git submodule pin commit (D-CROSS-002).",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
